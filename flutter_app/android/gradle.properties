org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Kotlin's incremental compiler can't relativize paths across different
# drive letters (project on D:, pub-cache on C:) and fails the build with
# "this and base files have different roots". Incremental compilation is
# just a build-speed optimization, so disabling it is a safe workaround.
kotlin.incremental=false
